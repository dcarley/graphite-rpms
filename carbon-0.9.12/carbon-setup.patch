diff -ur carbon-0.9.12.imeyer/setup.cfg carbon-0.9.12/setup.cfg
--- carbon-0.9.12.imeyer/setup.cfg  2013-09-15 18:53:31.009000963 +0000
+++ carbon-0.9.12/setup.cfg 2013-09-15 18:50:40.909001129 +0000
@@ -1,6 +1,3 @@
-[install]
-prefix = /opt/graphite
-install-lib = %(prefix)s/lib
 
 [bdist_rpm]
 requires = python-twisted
--- carbon-0.9.12.imeyer/setup.py	2013-08-21 16:52:35.000000000 +0000
+++ carbon-0.9.12/setup.py	2013-09-15 18:19:18.962001100 +0000
@@ -7,25 +7,17 @@
 if os.environ.get('USE_SETUPTOOLS'):
   from setuptools import setup
   setup_kwargs = dict(zip_safe=0)
-
 else:
   from distutils.core import setup
   setup_kwargs = dict()
 
+storage_dirs = [
+  ('/var/lib/carbon/whisper', []),
+  ('/var/lib/carbon/lists', []),
+  ('/var/lib/carbon/rrd', [])
+]
 
-storage_dirs = [ ('storage/whisper',[]), ('storage/lists',[]),
-                 ('storage/log',[]), ('storage/rrd',[]) ]
-conf_files = [ ('conf', glob('conf/*.example')) ]
-
-install_files = storage_dirs + conf_files
-
-# If we are building on RedHat, let's use the redhat init scripts.
-if platform.dist()[0] == 'redhat':
-    init_scripts = [ ('/etc/init.d', ['distro/redhat/init.d/carbon-cache',
-                                      'distro/redhat/init.d/carbon-relay',
-                                      'distro/redhat/init.d/carbon-aggregator']) ]
-    install_files += init_scripts
-
+install_files = storage_dirs
 
 setup(
   name='carbon',
